/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Initial from './app/container/Initial';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Initial);
